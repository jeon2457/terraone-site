
[중요] 웹페이지에서 index.html을 읽어들이는 기준폴더_깃허브_firebase Realtime Database


① GitHub Pages

GitHub Pages는 이 위치를 웹사이트의 루트로 사용합니다:

/(루트)
/index.html  ← 이 파일이 실제로 웹에서 열리는 시작점


👉 즉, GitHub에서는 public 폴더를 사용하지 않습니다.
저장소 최상단(index.html)이 실제 웹 루트가 됩니다.

🔹 사용자의 저장소 구조

https://github.com/jeon2457/terraone-site

이 경우 GitHub Pages는 다음을 웹으로 보여줍니다:

terraone-site/
  index.html   ← 웹사이트의 루트 파일


✔ GitHub Pages는 public 폴더를 무시합니다.
(특별히 "docs"나 다른 폴더로 바꾸지 않는 한 ==> Settings -> Pages -> main, docs 선택)

===========================================================
===========================================================

② Firebase Hosting

Firebase Hosting은 반드시 public 폴더를 기준으로 웹 호스팅합니다.

즉 Firebase Hosting의 웹 루트는:

public/
  index.html  ← 이것이 웹에서 보이는 첫 페이지


✔ Firebase Hosting은 반드시 public 폴더를 사용합니다
